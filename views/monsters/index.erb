<body>

<h1 class="title_div">Codex Monstrum</h1>
<div class="center_div">
<div class="blurb">
<p>Fearsome and formidable foes lurk within the hallowed halls where only the brave or stupid dare to tread.</p>

<p>Prepare yourself, for knowledge is power.</p>
</div>
</br>
<table class=table>
  <tr>
    <th>Name</th>
    <th></th>
    <th>Species</th>
    <th></th>
    <th>Type</th>
  </tr>
  <% for monster in @monsters %>
  <tr>
    <td><a href="/monsters/<%= monster.id %>"><%= monster.name.split.map(&:capitalize).join(' ')%></a></td>
    <td>|</td>
    <td><%= monster.species.split.map(&:capitalize).join(' ') %></td>
    <td>|</td>
    <td><%= monster.type.split.map(&:capitalize).join(' ')%></td>
  </tr>
  <% end %>
</div>
</body>
